# purtmars-plugins-v2

Purtmars Plugins 文档站（Fumadocs + Next.js），由 [purtmars-plugins](../purtmars-plugins)（Docusaurus）迁移而来。

## 开发

```bash
pnpm install
pnpm dev
```

## 从旧站重新同步内容

```bash
pnpm migrate:content
```

## 构建

```bash
pnpm build
pnpm start
```

部署目标与旧站一致：`https://plugins.ptms.ink`。